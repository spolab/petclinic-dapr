syntax = "proto3";

package com.toremo.petstore.owner.api.command;

option go_package = "github.com/spolab/petstore/owner/pkg/api/command";

import "schema/model.proto";

// Command to register a new owner
message RegisterOwner {
    model.Owner owner = 1;
}